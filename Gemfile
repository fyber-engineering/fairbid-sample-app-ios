ruby ">= 2.3.1"

source 'https://rubygems.org' do
    gem "cocoapods", '>= 1.5.3', :require => true
    gem "xcodeproj", '>= 1.5.7', :require => true ### must be one of the next: {>= 1.5.7, (> 1.5.1, < 1.5.5)}
end
### TODO: put `bundle exec` where is needed;
